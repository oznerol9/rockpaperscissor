/* create array with computer moves and function to select a random option */

var computerPlay = ['rock', 'paper', 'scissors'];

function randomPlay() {
    var randomNum = Math.floor(Math.random() * 3);
    return(randomNum)
}

computerPlay[randomPlay()]

/* function to play one round of rock paper and scissor */

function play() {
    
}